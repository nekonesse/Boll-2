function player_warping(){
	//THIS SUCKS!!!!!!!!!
	/*var pipecoll=instance_place(x,y+1,oPipe)
	if (down) && !(piped) && (grounded) && (pipecoll && pipecoll.pipe_direction==directions.up) {
		warp_in_pipe(pipecoll, 2, directions.up)
	}
	
	function warp_in_pipe(obj,spd,dir) {
		piped=true;
		var dir_calc = 90 + 90*dir
		x+=lengthdir_x(spd,dir_calc)
		y+=lengthdir_y(spd,dir_calc)
		
		with(obj) {
			if collision_rectangle(x-2,y-2,x+2,y+2,other,false,true) {
				global.warp_timer=max(0,global.warptimer--)
				if !(global.warp_timer) {
					var found = noone;
					var pipes = [];
					for (var i = 0; i < instance_number(oPipe); ++i;)
					{
						pipes[i] = instance_find(oPipe,i);
						if pipes[i].warpname == warptarget {
							found=pipes[i]
							break;
						}
					}
					other.x=pipes
				}
			}
		}
	}
	
	function warp_out_pipe(obj,spd,dir) {
	
	}*/
}